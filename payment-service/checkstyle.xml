<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
  "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
  "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
  <!-- Устанавливаем кодировку файлов -->
  <property name="charset" value="UTF-8"/>

  <!-- Проверка длины строки ВНЕ TreeWalker -->
  <module name="LineLength">
    <property name="max" value="120"/>
    <property name="ignorePattern" value="^package.*|^import.*|^ *\* *https?://.*"/>
  </module>

  <!-- Проверка наличия новой строки в конце файла -->
  <module name="NewlineAtEndOfFile"/>

  <!-- TreeWalker проверяет структуру Java-кода -->
  <module name="TreeWalker">
    <!-- Проверка пробела после запятых и других символов -->
    <module name="WhitespaceAfter">
      <property name="tokens" value="COMMA, SEMI, TYPECAST"/>
    </module>

    <!-- Проверка пробелов вокруг операторов -->
    <module name="WhitespaceAround">
      <property name="tokens" value="ASSIGN, BAND, BAND_ASSIGN, BOR, BOR_ASSIGN, BSR, BSR_ASSIGN, BXOR, BXOR_ASSIGN, DIV, DIV_ASSIGN, EQUAL, GE, GT, LAND, LE, LITERAL_ASSERT, LITERAL_CATCH, LITERAL_DO, LITERAL_ELSE, LITERAL_FINALLY, LITERAL_FOR, LITERAL_IF, LITERAL_RETURN, LITERAL_SYNCHRONIZED, LITERAL_TRY, LITERAL_WHILE, LOR, LT, MINUS, MINUS_ASSIGN, MOD, MOD_ASSIGN, NOT_EQUAL, PLUS, PLUS_ASSIGN, QUESTION, SL, SL_ASSIGN, SR, SR_ASSIGN, STAR, STAR_ASSIGN, TYPE_EXTENSION_AND"/>
    </module>

    <!-- Проверка на финальные локальные переменные (ОПЦИОНАЛЬНО, можно отключить) -->
    <module name="FinalLocalVariable"/>

    <!-- Проверка на неиспользуемые импорты -->
    <module name="UnusedImports"/>

    <!-- Проверка отступов -->
    <module name="Indentation">
      <property name="basicOffset" value="4"/>
      <property name="braceAdjustment" value="0"/>
      <property name="caseIndent" value="4"/>
      <property name="throwsIndent" value="4"/>
      <property name="arrayInitIndent" value="4"/>
      <property name="lineWrappingIndentation" value="4"/>
      <property name="forceStrictCondition" value="true"/>
    </module>

    <!-- Дополнительные полезные проверки -->
    <module name="EmptyBlock">
      <property name="option" value="TEXT"/>
    </module>

    <module name="LeftCurly"/>
    <module name="RightCurly"/>

    <module name="NeedBraces"/>

    <module name="AvoidStarImport"/>

    <!-- Javadoc только для публичных методов -->
    <module name="MissingJavadocMethod">
      <property name="scope" value="public"/>
      <property name="allowMissingPropertyJavadoc" value="true"/>
    </module>
  </module>
</module>